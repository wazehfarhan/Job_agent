# Copy to .env.local for local dev (or add NEXT_PUBLIC_API_URL to .env for compose).
# The browser calls the API directly, so this must be reachable from YOUR
# browser — localhost is right for local runs.
NEXT_PUBLIC_API_URL=http://localhost:8000